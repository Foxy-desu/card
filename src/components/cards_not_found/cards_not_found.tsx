import cl from './cards_not_found.module.scss';

const CardsNotFound = () => {
    return (
        <p className={cl.cardsNotFound}>
            Карточки не найдены
        </p>
    );
};

export default CardsNotFound;